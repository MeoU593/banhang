@model Nop.Plugin.Misc.DocumentPortal.Models.Admin.DocumentModel

@{
    Layout = "_AdminLayout";
    ViewBag.PageTitle = "Sửa tài liệu";
}

<form asp-controller="DocumentPortalAdmin" asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />
    <div class="content-header clearfix">
        <h1 class="float-left">Sửa tài liệu - @Model.Title</h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-save"></i> Lưu
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-save"></i> Lưu &amp; tiếp tục
            </button>
            <span id="document-delete" class="btn btn-danger">
                <i class="far fa-trash-alt"></i> Xóa
            </span>
            <a asp-action="List" class="btn btn-default">
                <i class="fas fa-arrow-left"></i> Quay lại
            </a>
        </div>
    </div>
    @await Html.PartialAsync("~/Plugins/Misc.DocumentPortal/Views/Admin/_CreateOrUpdate.cshtml", Model)
</form>
<nop-delete-confirmation asp-model-id="@Model.Id" asp-button-id="document-delete" asp-action="Delete" />
