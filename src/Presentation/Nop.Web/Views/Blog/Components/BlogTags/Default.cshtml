@model BlogPostTagListModel
@if (Model.Tags.Count > 0)
{
    <section class="block block-popular-blogtags overflow-hidden rounded-xl border border-outline-variant/30 bg-surface-container-lowest shadow-sm mb-4">
        <h2 class="title border-b border-outline-variant/20 bg-slate-900 px-4 py-3 text-xs font-semibold uppercase tracking-widest text-amber-400">
            @T("Blog.TagCloud")
        </h2>
        <div class="listbox p-4">
            <ul class="tags flex flex-wrap gap-2">
                @foreach (var item in Model.Tags)
                {
                    <li><a href="@Url.RouteUrl(NopRouteNames.Standard.BLOG_BY_TAG, new { tag = item.Name })" style="font-size:@(Model.GetFontSize(item))%;" class="inline-flex items-center rounded-full border border-outline-variant/40 bg-surface-container px-3 py-1 text-xs font-semibold text-on-surface-variant transition hover:border-military-green hover:text-military-green">@item.Name</a>
                    </li>
                }
            </ul>
        </div>
    </section>
}
