@model CatalogProductsModel

@{
    Layout = "_ColumnsOne";

    NopHtml.AddTitleParts("Sản phẩm / Năng lực");
    NopHtml.AppendPageCssClassParts("html-all-products-page");
}

<div class="page all-products-page bg-slate-100 px-4 py-6 lg:px-6 lg:py-8">
    <div class="page-body mx-auto w-full max-w-[1400px] space-y-6">
        <header class="rounded-2xl border border-slate-200 bg-white px-5 py-5 shadow-[0_18px_40px_-28px_rgba(15,23,42,0.18)] sm:px-6">
            <nav class="mb-3 flex flex-wrap items-center gap-2 text-xs font-semibold uppercase tracking-[0.16em] text-slate-500" aria-label="Breadcrumb">
                <a href="@Url.RouteUrl(NopRouteNames.General.HOMEPAGE)" class="transition hover:text-slate-700">Trang chủ</a>
                <span class="material-symbols-outlined text-sm text-slate-400">chevron_right</span>
                <span class="text-slate-700">Sản phẩm / năng lực</span>
            </nav>
            <p class="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500">Danh mục tổng hợp</p>
            <h1 class="mt-2 text-2xl font-black tracking-tight text-slate-900 sm:text-3xl">Toàn bộ sản phẩm / năng lực</h1>
            <p class="mt-2 text-sm leading-6 text-slate-600">Danh sách dữ liệu tổng, có phân trang để tránh tràn dữ liệu khi số lượng sản phẩm lớn.</p>
        </header>

        <section class="space-y-5">
            <div class="sticky top-4 z-20 rounded-2xl bg-slate-100/90 pb-1 pt-1 backdrop-blur supports-[backdrop-filter]:bg-slate-100/75">
                @await Html.PartialAsync("_CatalogSelectors", Model)
            </div>

            @{
                var catalogProductsViewData = new ViewDataDictionary(ViewData)
                {
                    ["PaginationSummaryLabel"] = "sản phẩm trong hệ thống"
                };
            }

            @await Html.PartialAsync("_CatalogProducts", Model, catalogProductsViewData)
        </section>
    </div>
</div>
